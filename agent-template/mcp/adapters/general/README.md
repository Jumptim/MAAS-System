# General MCP Adapter

Use `server.py` for a regular MCP client or local development.

This adapter shares the same tool registry, schemas, and tool implementations
as the Codex adapter. Change tool behavior under `../../shared/`, not here.
